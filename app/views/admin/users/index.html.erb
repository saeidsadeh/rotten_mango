<% @users.each do |user| %>
  <%= link_to "Edit a user!", edit_admin_user_path(user) %>
  <%= link_to "Delete a user!", admin_user_path(user), method: :delete, data: {confirm: "Are you sure?"} %>
  <h3><%= user.firstname %> <%= user.lastname %> </h3>
  <hr>
<% end %>